export class Category {
  constructor(
    public name: string,
    public description?: string,
    public is_active?: boolean,
    public create_at?: Date
  ) {}
}


/* uuid id
string name
string description
boolean is_active
date created_at
 */
